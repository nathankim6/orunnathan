import type {CSSProperties} from 'react';
export default function ReportSectionHead({kicker,title,tone='--gold',className=''}:{kicker:string;title:string;tone?:string;className?:string}){return <div className={`rp-section-head report-infographic-section-head ${className}`} style={{'--sec':`var(${tone})`} as CSSProperties}><div><span className="rp-section-kicker"><i/>{kicker}</span><h2 className="rp-section-title">{title}</h2></div><span className="ed-section-arrow" aria-hidden="true">↗</span></div>}
